# Recovery Audit — 2026-03-04

## Что произошло
- В логах подтверждён rollback без явного второго шага подтверждения:
  - `2026-03-04T00:44:31.458287+00:00` — `self_updater` `rollback_success`
  - backup: `/home/vito/vito-agent/backups/backup_20260303_155208`
  - `2026-03-04T00:44:31.529278+00:00` — `comms_agent` `cmd_rollback`

## Что восстановлено
1. Код восстановлен до состояния последнего стабильного коммита (`05c0969`) и проверен тестами.
2. Восстановлены runtime-переменные из последнего пред-rollback snapshot:
   - источник: `backups/20260303_220205/.env`
3. Устранён риск повторного тихого отката:
   - `/rollback` теперь требует явного подтверждения (`/rollback yes` или `да` на запрос).
4. Устранены лишние авто-подтверждения целей:
   - при `OWNER_AUTO_APPROVE_GOALS=true` LLM больше не может принудительно включить `needs_approval=true`.
5. Добавлены регрессионные тесты на оба сценария.
6. Добавлено правило частых коммитов в `AGENTS.md`.

## Что показали логи дополнительно
- Были ошибки `429 RESOURCE_EXHAUSTED` по Gemini free tier (временное исчерпание квоты).
- В момент инцидента фиксировалась ошибка `no such table: goal_workflows` в старом процессе; текущая БД содержит нужные таблицы.

## Ограничения восстановления
- Точные незакоммиченные изменения, если они были только в рабочем дереве и не попали в backup/commit, восстановить 1:1 невозможно.
- Доступно детерминированное восстановление только из:
  - git-коммитов,
  - сохранённых backup-снимков,
  - логов/аудита событий.

## Текущий статус
- Основной процесс `main.py` запущен.
- Критичный сценарий rollback защищён двухэтапным подтверждением.
- Базовая проверка тестов пройдена.
